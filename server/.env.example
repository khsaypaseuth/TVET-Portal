DB_HOST=localhost
DB_PORT=5432
DB_NAME=tvet_portal
DB_USER=postgres
DB_PASSWORD=change-me

JWT_SECRET=change-me-in-production
JWT_EXPIRE=7d

PORT=5001
NODE_ENV=development

# Optional: absolute path for uploads (defaults to server/uploads)
# UPLOAD_DIR=
